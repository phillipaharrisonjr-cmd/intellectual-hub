{
  "name": "denali-backend",
  "version": "1.1.0",
  "description": "Denali backend: ACH intelligence, revenue projections, referrals, and the Denali assistant",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:replay": "vitest run test/projection.replay.test.js",
    "lint": "eslint .",
    "prepare": "cd .. && husky backend/.husky 2>/dev/null || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^2.1.0",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "lint-staged": {
    "*.js": "eslint --fix"
  }
}
